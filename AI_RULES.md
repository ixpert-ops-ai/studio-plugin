# AI_RULES.md

## 📌 목적

이 문서는 Codex, Gemini 등 보조 AI가 프로젝트 구조를 깨지 않고 작업하도록 하기 위한 최소 규칙이다.

---

## 1. 아키텍처 규칙

이 프로젝트는 Agent 기반 구조를 따른다.

레이어 구조:

* ui/
* action/
* agent/
* service/
* prompt/
* setting/
* model/
* util/

---

## 2. 레이어 역할

* **agent/**

  * LLM 호출 및 전체 흐름 제어
  * 핵심 로직 담당

* **service/**

  * IDE API, 파일 처리 담당
  * LLM 호출 절대 금지

* **ui/action/**

  * UI 및 이벤트 처리만 수행
  * 비즈니스 로직 금지

---

## 3. LLM 호출 규칙

* 모든 LLM 호출은 반드시 agent 레이어에서만 수행
* service / util / ui에서 HTTP 호출 금지
* 프롬프트는 반드시 외부 파일 사용 (하드코딩 금지)

---

## 4. 작업 원칙

* 기존 구조를 절대 깨지 말 것
* 최소 변경 원칙 유지
* 기존 코드 수정은 반드시 필요한 경우만 수행
* 새로운 기능은 확장 형태로 추가

---

## 5. 역할 제한 (중요)

### Codex

* 기능 구현만 수행
* Service, Util, Model 생성 가능
* ❌ Agent 흐름 변경 금지
* ❌ 구조 변경 금지

### Gemini

* 코드 리뷰 및 검수
* 테스트 케이스 생성
* ❌ 구조 변경 제안 금지

---

## 6. 코드 스타일

* Kotlin idiomatic 코드 사용
* null-safe 처리 필수
* 불필요한 클래스/파일 생성 금지

---

## 7. 결과 생성 규칙

* 에러와 성공 응답은 반드시 구분
* 에러 시:

  * Diff 생성 금지
  * Apply 금지
  * Pipeline 중단

---

## 8. 핵심 원칙

이 프로젝트는 단순 기능 구현이 아닌
Agent 기반 시스템이다.

따라서:

* 기능보다 구조를 우선한다
* 실행보다 안정성을 우선한다
* 자동화보다 제어 가능성을 우선한다

---

## 📌 한 줄 요약

"구조를 깨지 않고, 최소 변경으로 기능을 확장하라"
